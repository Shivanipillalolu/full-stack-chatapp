apiVersion: v1
kind: Service
metadata:
  name: mongodb
  namespace: chat-app
spec:
  selector:
    app: mongodb      # match the Deployment’s label
  ports:
    - protocol: TCP
      port: 27017
      targetPort: 27017
